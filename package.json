{
  "name": "netlify_telegram_restaurant_bot",
  "version": "1.0.0",
  "description": "Node.js backend deployable to Netlify to host telegram bot's backend for University of Oulu restaurants",
  "main": "express\\server.js",
  "scripts": {
    "start": "node server-local.js",
    "build": "netlify-lambda build express",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Tomi Lämsä",
  "license": "MIT",
  "devDependencies": {
    "body-parser": "^1.18.3",
    "express": "^4.16.3",
    "netlify-lambda": "^0.4.0",
    "serverless-http": "^1.6.0"
  },
  "dependencies": {
    "axios": "^0.18.0"
  }
}
